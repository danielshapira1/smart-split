name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
      - run: npm ci || npm install
      - name: Build
        run: npm run build
        env:
          VITE_SUPABASE_URL: ${{https://huajawlbpipdgseooppm.supabase.co}}
          VITE_SUPABASE_ANON_KEY: ${{eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imh1YWphd2xicGlwZGdzZW9vcHBtIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTYzOTUzNjcsImV4cCI6MjA3MTk3MTM2N30.2ho1KLVt5a-BiXH-4ku0XR6ZRjgUO-M0pXiwc_7r2Ko}}
      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
